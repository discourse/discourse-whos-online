{{#if shouldDisplay }}
  <div id="whos-online" class={{if isLong "collapsed"}}>
    <span title={{i18n "whos_online.tooltip"}}>
      {{#if hasUsers }}
        {{i18n "whos_online.title" count=count}}
      {{else}}
        {{i18n "whos_online.no_users"}}
      {{/if}}
    </span>
    {{#if hasUsers }}
      {{#each users as |user|}}
        {{whos-online-avatar user=user}}
      {{/each}}
    {{/if}}
  </div>
{{/if}}
